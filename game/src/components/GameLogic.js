export default function X() {
  console.log("hi");
}

let apiImg = [
  {id:1, img:imgURl}
  {id:4, img:imgURl}
  {id:3, img:imgURl}
  {id:2, img:imgURl}
]

let gameArray = apiImg.concat(apiImg)

//shuffle the array 

// render the shufffled array into cards each card will take id + img  
// if id matches keep the card displied else flipback 
//